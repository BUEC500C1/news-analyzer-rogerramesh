"""
Uploads file to the system that can be converted to .txt so it can be read by NLP
"""
#method will check if there is compatibility
def check():
    """
    check to see if the file being uploaded is a compatible type
    """
#method will convert the file
def convert():
    """
    file will be converted to a text type so it can be parsed
    """